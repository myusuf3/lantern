# packet

An IP header plus its payload. The packet is what travels end to end: its source and destination IP addresses stay the same across every hop, even as the frames carrying it are replaced at each one.
