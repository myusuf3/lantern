# router

A machine with an interface on two or more subnets whose job is to carry packets between them. It receives a frame addressed to its own MAC, looks at the IP destination inside, lowers the TTL, and sends the packet out again in a new frame on the right cable. Every home has one; the internet is a few hundred thousand of them.
