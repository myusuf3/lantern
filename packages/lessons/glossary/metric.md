# metric

A number on a routing table row that says how costly that path is, as far as the router is concerned. When two rows match a destination equally well, the lower metric wins. It is set by configuration or by a routing protocol, not measured on the wire, and changing it re-routes packets without touching a cable.
