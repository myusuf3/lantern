# ARP table

A machine's memory of which IP address lives at which MAC address, one row per neighbour it has heard from. Empty on a fresh boot. Filled by ARP replies, and by ARP requests it happens to overhear. Also called the neighbour table, as in `ip neigh`.
