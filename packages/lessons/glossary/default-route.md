# default route

The routing table entry that matches any destination, written `0.0.0.0/0`. It is what a machine uses when nothing more specific matches: send it to the gateway and let the gateway worry. On a laptop, almost everything goes this way.
