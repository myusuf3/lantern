# Wireshark

The standard tool for looking inside network traffic. It reads capture files and shows every frame decoded, header by header, the way the frame inspector in these lessons does. It is free, it runs everywhere, and it is what network engineers actually use when something is wrong. `tshark` is the same program without the window.
