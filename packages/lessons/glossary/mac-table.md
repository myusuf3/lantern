# MAC table

A switch's memory: which MAC address was last seen arriving on which port. Filled by watching the source address of every frame. A destination that is in the table goes out one port; one that is not goes out all of them.
