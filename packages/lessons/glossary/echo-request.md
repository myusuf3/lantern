# echo request

The ICMP message ping sends: "are you there? send this back". It carries an identifier and a sequence number so the reply can be matched to the request, and a payload the other side must return unchanged.
