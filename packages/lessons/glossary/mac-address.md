# MAC address

The address burned into a network card at the factory, written as six pairs like `02:00:00:00:00:01`. Ethernet delivers frames to MAC addresses, never to IP addresses. Two machines on the same cable talk by MAC, whatever their IPs say.
