# switch

A box with several ports that joins cables into one network without joining subnets. It has no IP address of its own. It learns which MAC address is behind which port by watching frames go by, and uses that to send each frame out only the port that needs it. Everything on a switch is one subnet; crossing subnets is the router's job.
