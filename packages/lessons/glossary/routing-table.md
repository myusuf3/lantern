# routing table

The list a machine consults before sending anything: for this destination, use this interface, and hand it to this next machine. The simplest entry says "that subnet is on this cable, send directly". As in `ip route`.
