# frame

What actually travels on a cable: an Ethernet header with a source MAC and a destination MAC, followed by whatever is being carried. A frame lives for exactly one hop. Cross a router and you get a new frame with new MACs.
