# flooding

What a switch does with a frame whose destination it has not learned yet, or which is addressed to everyone: send it out every port except the one it came in on. It is the safe default when the switch does not know better, and it is how a broadcast reaches all machines.
