# subnet

A block of IP addresses that share a prefix, written like `10.0.1.0/24`: every address that starts with `10.0.1`. Machines on the same subnet can reach each other directly. Anything outside it has to go through a router.
