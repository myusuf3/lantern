# IP address

A number that names a machine on a network, written as four numbers like `10.0.1.10`. It is the address the rest of the world uses. It is assigned by configuration, not built into the hardware, and it can change.
