That is the whole story for two machines on one wire: find the route, resolve the MAC, send, reply. Download the capture below and open it in Wireshark. You will find exactly these frames, byte for byte, because this was a real simulation, not a picture of one.

Every lesson from here adds one box to this picture. The next one puts a router in the middle, and the MAC addresses start to change while the IP addresses do not.
