You have run `traceroute`. Here is what it is made of: scene 2, repeated. Send a ping with TTL 1 and see who complains. Then TTL 2. Then 3. Each complaint comes from one router further along, from the address of the interface your packet arrived on, and the list of complainers is the path.

Same network as the last scene, so the short way. Watch the laptop build the list.
